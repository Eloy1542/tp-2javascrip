let palabraIntroducida = prompt("Introduzca una palabra o texto")

for (let i = 0 ; i < palabraIntroducida.length ; i++) {


	switch(palabraIntroducida.charAt(i)) {

		case "a":
		document.write(palabraIntroducida.charAt(i))
		break;

		case "e":
            document.write(palabraIntroducida.charAt(i))
		break;

		case "i":
            document.write(palabraIntroducida.charAt(i))
		break;

		case "o":
            document.write(palabraIntroducida.charAt(i))
		break;

		case "u":
            document.write(palabraIntroducida.charAt(i))
		break;

		default:
		
		break;

	}

}


document.write(palabraIntroducida.charAt());
 